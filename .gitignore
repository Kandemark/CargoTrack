# ── Python ──────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
eggs/
.eggs/
*.manifest
*.spec

# ── Virtual environments ────────────────────────────────────────────────────────
.venv/
venv/
env/
.env/
.venv-*/

# ── Django ───────────────────────────────────────────────────────────────────────
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
media/
staticfiles/
collected-static/

# Generated OpenAPI schema (rebuilt via `python manage.py spectacular --file schema.openapi.yml`)
schema.openapi.yml

# Django migrations — regenerated per-environment via `makemigrations`
**/migrations/0*.py

# ── Environment & secrets ───────────────────────────────────────────────────────
.env
.env.*
!.env.example
.env.production
.env.staging
.envrc
*.token
secrets/
*.secret

# ── Certificates & private keys ─────────────────────────────────────────────────
*.pem
*.key
*.crt
*.cer
*.der
*.p12
*.pfx
*.pkcs12
*.pkcs8
*.mobileprovision
*.p8
*.ppk
id_rsa
id_ed25519
id_ecdsa
id_dsa

# ── Database dumps & backups ────────────────────────────────────────────────────
*.dump
*.pgdump
*.sql.gz
*.sql.bz2
dump.*
*.backup.sql
backup/

# ── IDE & editor ─────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*.swn
*~
*.suo
*.ntvs*
*.njsproj
*.sln

# ── OS files ─────────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini
desktop.ini
$RECYCLE.BIN/

# ── Backup & temp files ─────────────────────────────────────────────────────────
*.bak
*.tmp
*.temp
*.orig
*.old
*.backup
*.orig.*

# ── Node / Frontend ─────────────────────────────────────────────────────────────
node_modules/
*.tsbuildinfo

# Frontend env files
frontend/.env
frontend/.env.local
frontend/.env.*.local

# ── Testing & coverage ──────────────────────────────────────────────────────────
.coverage
coverage/
htmlcov/
.pytest_cache/
.tox/
.nox/
*.cover
.hypothesis/
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
coverage.xml
junit.xml
test-results/
.pytest_cache/

# ── ML models & datasets ────────────────────────────────────────────────────────
*.joblib
*.pkl
*.pickle
*.h5
*.hdf5
*.pb
*.onnx
*.pt
*.pth
ml_datasets/

# ── Docker ───────────────────────────────────────────────────────────────────────
docker-compose.override.yml
docker/secrets/
*.docker-secret
data/

# ── Mobile / Expo ───────────────────────────────────────────────────────────────
mobile/.env
mobile/.env*.local
mobile/dist-test/
mobile/dist-test-verify/
mobile/dist/
mobile/android/
mobile/ios/
mobile/.expo/
mobile/web-build/
mobile/.kotlin/
mobile/*.jks
mobile/*.keystore
mobile/*.p8
mobile/*.p12
mobile/*.key
mobile/*.mobileprovision
mobile/credentials.json
mobile/google-services.json
mobile/GoogleService-Info.plist
mobile/.metro-health-check*

# ── Claude / internal tooling ───────────────────────────────────────────────────
weekly_reports/
.claude/
.claudeignore

# ── Monorepo root stubs (Metro bundler workarounds) ───────────────────────────
/index.js
/babel.config.js
/app.json
/package.json
/package-lock.json

# ── Test / debug scratch ────────────────────────────────────────────────────────
test_users.md
scratch/
debug/
tmp/
temp/
smoke_test.py
smoke_test_*.py

# ── Elixir / Phoenix ───────────────────────────────────────────────────────────
elixir-ws/_build/
elixir-ws/deps/
erl_crash.dump
*.ez

# ── Go ──────────────────────────────────────────────────────────────────────────
services/tracking-ingest/tracking-server.exe
services/tracking-ingest/tracking-server
services/notification/notification.exe
services/notification/notification
services/webhook-dispatcher/webhook-dispatcher.exe
services/webhook-dispatcher/webhook-dispatcher

# ── Java / Maven ───────────────────────────────────────────────────────────────
*.class
*.jar
*.war
*.ear
!.mvn/wrapper/maven-wrapper.jar
# Maven wrapper downloads
.mvn/wrapper/MavenWrapperDownloader.java

# ── Rust ────────────────────────────────────────────────────────────────────────
# Build output (all service crates)
**/target/
# Compiled binaries
services/gps-ingest/gps-ingest.exe
services/gps-ingest/gps-ingest
services/route-optimizer/route-optimizer.exe
services/route-optimizer/route-optimizer
services/fuel-optimizer/fuel_optimizer_rs.dll
services/fuel-optimizer/fuel_optimizer_rs.so
services/fuel-optimizer/fuel_optimizer_rs.dylib
services/container-matcher/container_matcher_rs.dll
services/container-matcher/container_matcher_rs.so
services/container-matcher/container_matcher_rs.dylib
# Rust-generated files
*.rs.bk
# PyO3 extension modules copied into Django (built artifacts, not source)
services/api/cargotrack/ml/*.pyd
services/api/cargotrack/ml/*.so
services/api/cargotrack/ml/*.dylib
