/**
 * @cargotrack/design-tokens
 *
 * Canonical design tokens for CargoTrack.
 * Single source of truth for colors, spacing, typography, and shadows
 * across all platforms (mobile, web, and beyond).
 *
 * Usage:
 *   import { brand, spacing, radius, light, dark } from '@cargotrack/design-tokens'
 */

export { brand, status, risk, ui, map } from './colors'
export { light, dark } from './surfaces'
export { spacing, radius } from './spacing'
export { family, size, weight } from './typography'
export { card as cardShadow, sheet as sheetShadow, fab as fabShadow } from './shadows'
